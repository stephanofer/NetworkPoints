package com.stephanofer.networkpoints;

import io.papermc.paper.plugin.bootstrap.BootstrapContext;
import io.papermc.paper.plugin.bootstrap.PluginBootstrap;

/**
 * Paper bootstrap entry point. Command registration is added here in block 4.
 */
public final class NetworkPointsBootstrap implements PluginBootstrap {

    @Override
    public void bootstrap(BootstrapContext context) {
        // Block 1 reserves the correct bootstrap boundary without registering later features.
    }
}
